<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apostamos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ListaUsuarioPrueba"></activity>
        <activity android:name=".ListaUsuarios" />
        <activity android:name=".NuevaApuesta" />
        <activity
            android:name=".MenuPrincipal"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AdministradorUsuario"
            android:theme="@style/AppTheme" />
        <activity android:name=".Registrar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>